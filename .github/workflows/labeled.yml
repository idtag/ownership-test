name: Labeled

on:
  pull_request:
    types:
      - labeled
jobs:
  notify:
    name: Notify
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v3
      - name: Notify
        uses: ./
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          mapping: >
            - label: team1
              message: 'Hey !{{ recipients }}, assess components were touched!'
              recipients:
                - '@idtag'
